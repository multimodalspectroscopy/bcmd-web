sudo: false
language: python
python:
  - "3.5.2"

# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: ./confgure && make build/rc.model && cd app/BayesCMD/testing && nosetests --nocapture --with-coverage